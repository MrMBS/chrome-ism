/* jshint maxlen:false */
define([], function () {
  return {
    boardNotConfigured: 'Board collection not configured with a page handler.',
    nullCookieSet: 'Chrome cookies api returned null on its set.',
    invalidServerRole: 'Supplied an invalid server role to pagehandlers.get().'
  };
});